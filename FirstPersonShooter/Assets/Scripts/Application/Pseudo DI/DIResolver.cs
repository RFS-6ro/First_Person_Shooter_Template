namespace DI
{
    public class DIResolver
    {
        //var ignoreType = typeof(IgnoreInjectAttribute);
        //foreach (var field in class.GetFields(BindingFlags.Public | BindingFlags.NonPublic | BindingFlags.Instance))
        //{
        //  if (field.IsStatic || Attribute.IsDefined(field, ignoreType))
        //  {
        //      continue;
        //  }
        //}
    }
}
